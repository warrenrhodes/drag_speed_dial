name: drag_speed_dial
description: Flutter package to implement a FAB and dynamic Material Design Speed Dial, draggable, multi-directional children and much more.
version: 1.2.0
homepage: https://github.com/warrenrhodes/drag_speed_dial

environment:
  sdk: ">=3.9.2 <4.0.0"
  flutter: ">=3.35.6"

dependencies:
  flutter:
    sdk: flutter

  widget_tooltip: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
